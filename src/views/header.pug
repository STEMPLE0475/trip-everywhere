header
    header
        a(href="/").header__logo
            i.fab.fa-youtube
        
        nav
            ul
                li
                    a(href="/search")
                        i.fas.fa-search
                if loggedIn
                    li
                        a(href=`/user/${loggedInUser._id}`) My profile
                    li
                        a(href="/user/edit") Edit Profile
                    li
                        a(href="/user/logout") Log Out
                    li
                        a(href="/video/upload") Upload
                else
                    li
                        a(href="/login") Log In
                    li
                        a(href="/register").header__btn Register
                    
                
        if message
            h1=message

        aside
            ul
                if loggenIn
                    span Welcome!!
                    span #{loggedInUser.name}
                else
                
                li
                    a(href="like") Like
                li
                    a(href="user") User
                li
                    a(href="subscribe") Subscribe 